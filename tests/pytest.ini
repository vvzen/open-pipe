[pytest]
markers =
    to_test: debugging marker used to filter specific tests during a testing session
    smoke_test: doesn't test behaviour, only tests that code runs
    behaviour_test: test behaviour and ensures not regressions in features
    stress_test: test behaviour outside the expected usage (garbage in, etc.)
